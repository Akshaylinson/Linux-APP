class AppConstants {
  static const String appName = 'SystemLens';
  static const String fullAppName = 'SystemLens — Linux System Dashboard';
  static const Duration defaultRefresh = Duration(seconds: 2);
  static const Duration storageRefresh = Duration(seconds: 8);
  static const Duration dockerRefresh = Duration(seconds: 8);
  static const Duration hardwareRefresh = Duration(seconds: 20);
}
